query getDentists($input: GetDentistsInput!) {
  getRightsPortalDentists(input: $input) {
    currentDentistName
    billHistory {
      number
      amount
      coveredAmount
      date
      refundDate
    }
  }
}
